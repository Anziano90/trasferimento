package com.example.trasferimento.repositories;

import com.example.trasferimento.entities.EsamiPdsEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EsamiPdsRepository extends JpaRepository<EsamiPdsEntity, Long> {
}