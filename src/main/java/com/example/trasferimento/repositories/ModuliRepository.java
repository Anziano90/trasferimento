package com.example.trasferimento.repositories;

import com.example.trasferimento.entities.ModuliEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModuliRepository extends JpaRepository<ModuliEntity, Long> {
}