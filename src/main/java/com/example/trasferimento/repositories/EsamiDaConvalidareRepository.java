package com.example.trasferimento.repositories;

import com.example.trasferimento.entities.EsamiDaConvalidareEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EsamiDaConvalidareRepository extends JpaRepository<EsamiDaConvalidareEntity, Long> {
}